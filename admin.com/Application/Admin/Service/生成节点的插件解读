这个类只负责计算分类的边界，和生成sql语句，但是不执行sql语句。
它开放了一个接口,里面定义了规则，告诉外界什么样的对象可以来执行我的sql语句。


用户只需要引入这个插件类和接口类。定义一个类来继承接口即可。
把插件类放在service中是因为它是用来处理业务逻辑的。在thinkphp中除了固定的那几个块之外，还可以定义logic等目录,
在thinkphp文档中的 架构-》模块化设计  可以找到


放在什么文件夹下面，就以什么结尾，这个tp的规则，请遵守
